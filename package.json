{
  "name": "cube-lab",
  "version": "1.0.0",
  "description": "CUBE LAB — Interactive 3D Multi-Puzzle Studio (Fullstack)",
  "type": "module",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "preview": "npm run preview --prefix client",
    "server": "npm start --prefix server",
    "server:dev": "npm run dev --prefix server"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
